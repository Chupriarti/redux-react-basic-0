const SET_COUNT = "SET_COUNT";

const defaultState ={
    items: [],
    iFetching: true
}

export default function reposReducer(state = defaultState, action){
    switch (action.type) {
        default: 
            return state;
    }
}
