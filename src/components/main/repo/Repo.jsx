import React from 'react';
import './repo.less'

const Repo = (props) => {
    const repo = props.repo;

    return (
        <div className="repo">
            Repo
        </div>
    )
}

export default Repo;
